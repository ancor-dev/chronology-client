/* tslint:disable:no-unused-variable */import { ComponentFixture, TestBed, async, inject, tick, fakeAsync } from '@angular/core/testing';import { DebugElement, Component, Input, Output, EventEmitter } from '@angular/core';import { By } from '@angular/platform-browser';// Load the implementations that should be testedimport { ImgCoverComponent } from './img-cover.component';@Component({  template: `    <app-img-cover></app-img-cover>  `,})class TestHostComponent {}describe('Component: ImgCoverComponent', () => {  let fixture: ComponentFixture<TestHostComponent>,      comp: TestHostComponent,      el: DebugElement;  // provide our implementations or mocks to the dependency injector  beforeEach(() => {    TestBed.configureTestingModule({      declarations: [        TestHostComponent,        ImgCoverComponent,      ],    });    fixture = TestBed.createComponent(TestHostComponent);    comp    = fixture.componentInstance;    el      = fixture.debugElement;    fixture.detectChanges();  });  it('Component element created successful', () => {    let compEl = el.query(By.css('app-img-cover'));    expect(compEl).toBeTruthy();  });});